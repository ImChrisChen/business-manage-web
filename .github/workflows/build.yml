name: "business-manage-web Project Build"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.6.0
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 18.12.1

      - name: Install And Build
        run: |
          npx pnpm install
          npm run build
          pwd
          ls -al

      - name: Commit And Push
        env:
          GITHUB_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          # GITHUB_TOKEN: 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
        run: |
          ls -al
          git status
          git --no-pager log
          cat ./package.json
          pwd

          git config --global user.email "imchrisorz@gmail.com"
          git config --global user.name "imchrisorz"
               
          # npm version patch
          # ver=$(cat ./package.json | grep -e '"version":' | tr -cd "[0-9].")
          # echo "${{ secrets.ACCESS_TOKEN }}"
          touch 1.txt
          echo "100" >> 1.txt
          
          git add .
          git commit -m "ci: 版本发布,当前版本"
          git status
          git --no-pager log
          cat ./package.json
          pwd
          
          # git push https://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@github.com/ImChrisChen/business-manage-web.git main

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: imchrisorz
          password: dckr_pat_UUoUPBvOuTi1_RQWsjdbqSUiZOA

      - name: Build Docker Image And Push Image
        run: |
          bash ./scripts/release.sh